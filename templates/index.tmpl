<!doctype html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport"
        content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link rel="stylesheet" href="templates/style.css">
  <title>{{ title }}</title>
</head>
<body>
  <h3>{{ title }}</h3>
  <div class="img-small">
  {% for img in images %}
      <div><a class="img" href="img.php?img={{img}}"> <img src="{{path_img_small}}/{{img}}" alt=""></a></div>
  {% endfor %}
  </div>
</body>
</html>